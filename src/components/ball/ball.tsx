import React from 'react';
import {View} from 'react-native';

const Ball = () => {
  return (
    <View
      style={{
        width: 60,
        height: 60,
        borderRadius: 30,
        backgroundColor: '#f00',
      }}></View>
  );
};

export default Ball;
